---
- hosts : all
  become : true
  tasks :
    - name : install apache2
      package :
        name : apache2
        state : present
      when : ansible_distribution == "Ubuntu"
    - name : execute command 
      command : ip a
      
    - name : removing httpd
      package : 
        name : httpd
        state : present
    
